import turtle

turtle.shape("turtle")

short_distance = 50
long_distance = 100
angle = 90

turtle.forward(long_distance)
turtle.left(angle)
turtle.forward(short_distance)
turtle.left(angle)
turtle.forward(long_distance)
turtle.left(angle)
turtle.forward(short_distance)
turtle.left(angle)


turtle.exitonclick()
